Included files:
main.cpp - Main executable. Reads in the file data.txt, displays the matrices it reads in, the decomposed matrices L and U,
	   the solution y from Ly=b, and the solution matrix x. It also outputs the residual.
Matrix.h - Matrix class declarations
Matrix.cpp - Matrix class definitions

To run:
Add all files into a project, compile, and run main.cpp